# Generated Results

Store generated, reproducible evidence here:

```text
results/
├── audit/
├── runs/<experiment_id>/<timestamp>/
├── plots/
├── confusion_matrix/
├── predictions/
└── error_analysis/
```

Never type result values manually into presentation materials without linking
them to a saved run artifact.
