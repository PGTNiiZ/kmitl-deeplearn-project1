# Model Weights

Store the best and backup checkpoints here. Each checkpoint package must include
or reference the architecture, preprocessing specification, 72-class label
mapping and its hash, validation metrics, experiment ID, and code/config version.

Large weight files should not be committed unless the submission rules require
it; document the delivery method and verify an offline copy before demo day.
